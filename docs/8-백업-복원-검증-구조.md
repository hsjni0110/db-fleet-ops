# 백업 복원 검증 구조

## 1. 이 문서의 범위

이 문서는 생성된 백업 파일을 임시 Database에 복원하고, 복원 결과로 백업 성공 여부를 확인하는 과정만 설명합니다.

- 백업 Job과 Task의 전체 연결은 [5번 문서](5-백업-Job과-Agent-Task-실행-구조.md)를 참고합니다.
- Agent의 Task 선점, 실행권과 결과 재전송은 [4번 문서](4-중앙-관제-서버와-Go-Agent-분리-구조.md)를 참고합니다.

## 2. 파일 생성만으로 부족한 이유

`mysqldump`가 종료되고 파일이 만들어졌다고 해서 실제로 복원할 수 있다는 뜻은 아닙니다. 파일이 잘렸거나 SQL이 손상됐거나 필요한 Table이 빠져 있어도 파일 자체는 존재할 수 있습니다.

따라서 백업 성공 기준을 두 단계로 나눴습니다.

```text
1단계  백업 파일 생성과 기본 형식 확인
2단계  임시 Database에 실제 복원하고 내용 확인
```

`verifyAfterBackup=false`일 때만 1단계 성공으로 Job을 끝냅니다. 기본 백업 흐름에서는 복원 검증 Task까지 성공해야 Job이 성공합니다.

## 3. 전체 검증 흐름

```text
MYSQL_LOGICAL_BACKUP 성공
  → MYSQL_RESTORE_VERIFY Task 생성
  → 임시 Database 생성
  → 백업 파일 복원
  → 복원된 Table 조회
  → 선택적으로 Row Count 확인
  → 검증 결과 보고
  → 임시 Database 정리
  → Job 성공 또는 실패
```

복원 검증 Task는 백업 파일이 있는 Host에서 실행되어야 하므로 최초 백업 Task와 같은 Agent에 배정됩니다.

## 4. 임시 Database 보호

복원 검증은 운영 Database를 덮어쓰지 않고 별도 임시 Database를 만듭니다. 이름이 비어 있으면 원본 이름, Job ID와 실행 시각을 이용해 만듭니다.

임시 Database 이름은 허용된 형식인지 검사하고, 생성·삭제 SQL에서는 식별자를 별도로 검증합니다. 정리는 복원 성공 여부와 관계없이 시도합니다.

```text
복원 실패 + 정리 성공 → 복원 실패
검증 실패 + 정리 성공 → 검증 실패
검증 성공 + 정리 실패 → CLEANUP_FAILED
```

정리 실패를 성공으로 숨기지 않는 이유는 임시 Database가 Host에 계속 남아 저장 공간과 운영 상태에 영향을 줄 수 있기 때문입니다.

## 5. 복원 내용 확인

`MySQLRestoreVerifier`는 임시 Database에서 Table 목록을 읽습니다.

- 복원된 Table이 하나도 없으면 실패합니다.
- 기대 Table이 지정되면 각 Table의 존재 여부를 확인합니다.
- 기대 Table이 없으면 복원된 전체 Table을 확인 대상으로 사용합니다.
- `verifyRowCount=true`이면 확인 대상 Table의 Row Count를 조회합니다.
- Table 누락이나 Count 실패가 하나라도 있으면 전체 검증을 실패로 처리합니다.

항목별 상태는 다음과 같습니다.

```text
VERIFIED      Table 존재와 선택한 검증을 통과
MISSING       기대 Table이 없음
COUNT_FAILED  Row Count 조회 실패
SKIPPED       Row Count 검증을 사용하지 않음
```

Row Count는 데이터 내용이 원본과 완전히 같다는 것을 증명하지 않습니다. 현재는 “복원이 실행되고 필요한 Table을 읽을 수 있다”는 수준의 검증입니다.

## 6. 검증 결과 저장과 Job 판정

Agent가 보낸 결과는 `BackupRestoreVerification`과 `BackupRestoreVerificationItem`에 저장합니다.

```text
VERIFIED       → Job SUCCEEDED
FAILED         → Job FAILED
CLEANUP_FAILED → Job FAILED
```

결과에는 원본 Database, 백업 파일, 임시 Database, 복원된 Table 수, 확인한 Table 수, Row Count 합계와 항목별 결과가 남습니다.

같은 Task 성공 결과가 다시 전송되면 Task 결과 멱등성 처리에 의해 검증 장부와 후속 작업을 다시 만들지 않습니다.

## 7. 조회 방법

복원 검증 결과는 다음 기준으로 조회할 수 있습니다.

- Job ID로 해당 백업의 검증 결과 조회
- Database ID로 가장 최근 검증 결과 조회
- 검증 ID로 상세 결과 조회

상세 응답에는 Table별 존재 여부와 Row Count 결과가 포함됩니다.

## 8. 주요 코드 책임

| 코드 | 책임 |
|---|---|
| `MySQLRestoreVerifyHandler` | 복원, 검증과 정리 순서 조정 |
| `MySQLRestoreRunner` | 임시 Database 생성·복원·삭제 |
| `MySQLRestoreVerifier` | Table과 선택적 Row Count 확인 |
| `BackupWorkflow` | 백업 성공 후 복원 검증 Task 생성과 Job 판정 |
| `RestoreVerifyTaskPayloadFactory` | 백업 결과를 복원 검증 입력으로 변환 |
| `BackupRestoreVerificationResultRecorder` | 검증 결과와 항목 저장 |
| `BackupRestoreVerificationQueryService` | Job·Database·검증 ID 기준 조회 |

## 9. 자동 테스트로 확인한 내용

- 임시 Database 생성, 백업 파일 복원과 정리 명령을 검증합니다.
- 잘못된 임시 Database 이름을 거절합니다.
- Table 없음, 기대 Table 누락과 Row Count 실패를 검출합니다.
- Row Count를 사용하지 않을 때 항목을 `SKIPPED`로 처리합니다.
- 검증과 정리 결과가 Handler 결과에 반영됩니다.
- 복원 검증 결과와 항목이 Metadata DB에 저장됩니다.
- 백업 성공 후 복원 검증 Task가 한 번만 생성되고 최종 결과가 Job에 반영됩니다.

## 10. 아직 보완하거나 실험할 부분

- 수십·수백 GB 백업을 실제로 복원하는 시간과 추가 저장 공간은 측정하지 않았습니다.
- 전체 Table의 Row Count는 큰 Database에서 매우 오래 걸릴 수 있습니다.
- 원본과 복원본의 Row Count, checksum 또는 표본 데이터를 직접 비교하지 않습니다.
- Trigger, Procedure, Event, View와 사용자 권한의 복원 여부를 별도로 검증하지 않습니다.
- 임시 Database 정리가 반복 실패할 때 자동으로 찾아 삭제하는 별도 회수기는 없습니다.
- 백업 파일을 Object Storage에서 내려받아 검증하는 흐름은 아직 없습니다.
