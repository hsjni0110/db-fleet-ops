apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: db-fleetops
  labels:
    app: prometheus
    component: observability
spec:
  type: ClusterIP
  selector:
    app: prometheus
    component: observability
  ports:
    - name: http
      port: 9090
      targetPort: http