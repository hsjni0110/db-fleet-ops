apiVersion: v1
kind: Service
metadata:
  name: metadata-mysql
  namespace: db-fleetops
  labels:
    app: metadata-mysql
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  selector:
    app: metadata-mysql