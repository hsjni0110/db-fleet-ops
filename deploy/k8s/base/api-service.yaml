apiVersion: v1
kind: Service
metadata:
  name: db-fleetops-api
  namespace: db-fleetops
  labels:
    app: db-fleetops-api
    component: api
spec:
  type: ClusterIP
  selector:
    app: db-fleetops-api
    component: api
  ports:
    - name: http
      port: 8080
      targetPort: http