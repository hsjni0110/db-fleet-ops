spring:
  application:
    name: db-fleetops

  datasource:
    url: ${DB_FLEETOPS_DATASOURCE_URL:jdbc:h2:mem:dbfleetops;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    driver-class-name: ${DB_FLEETOPS_DATASOURCE_DRIVER:org.h2.Driver}
    username: ${DB_FLEETOPS_DATASOURCE_USERNAME:sa}
    password: ${DB_FLEETOPS_DATASOURCE_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: ${DB_FLEETOPS_HIBERNATE_DIALECT:org.hibernate.dialect.H2Dialect}
    show-sql: false

  mvc:
    problemdetails:
      enabled: true

server:
  port: ${SERVER_PORT:8080}

db-fleetops:
  credential:
    encryption-key: ${DB_FLEETOPS_CREDENTIAL_ENCRYPTION_KEY}
  target:
    host: ${DB_TARGET_HOST:127.0.0.1}
    port: ${DB_TARGET_PORT:3306}
    database: ${DB_TARGET_NAME:dbops_target}
    username: ${DB_TARGET_USERNAME:db_monitor}
    password: ${DB_TARGET_PASSWORD}
    connect-timeout: ${DB_CONNECT_TIMEOUT:2s}
    socket-timeout: ${DB_SOCKET_TIMEOUT:2s}
  cors:
    allowed-origins: ${DB_FLEETOPS_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  task-lease:
    duration: ${DB_FLEETOPS_TASK_LEASE_DURATION:60s}
    renewal-interval: ${DB_FLEETOPS_TASK_LEASE_RENEWAL_INTERVAL:20s}
    expiration-check-interval: ${DB_FLEETOPS_TASK_LEASE_EXPIRATION_CHECK_INTERVAL:5s}
    maximum-attempts: ${DB_FLEETOPS_TASK_MAXIMUM_ATTEMPTS:3}
    expiration-batch-size: ${DB_FLEETOPS_TASK_EXPIRATION_BATCH_SIZE:100}
    reaper-enabled: ${DB_FLEETOPS_TASK_LEASE_REAPER_ENABLED:true}
  job-lease:
    duration: ${DB_FLEETOPS_JOB_LEASE_DURATION:60s}
    expiration-check-interval: ${DB_FLEETOPS_JOB_LEASE_EXPIRATION_CHECK_INTERVAL:5s}
    retry-delay: ${DB_FLEETOPS_JOB_RETRY_DELAY:30s}
    expiration-batch-size: ${DB_FLEETOPS_JOB_EXPIRATION_BATCH_SIZE:100}
    reaper-enabled: ${DB_FLEETOPS_JOB_LEASE_REAPER_ENABLED:true}

logging:
  pattern:
    console: >-
      %d{yyyy-MM-dd HH:mm:ss.SSS}
      %-5level
      [%thread]
      [requestId=%X{requestId:-none}]
      %logger{36}
      - %msg%n

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
      group:
        liveness:
          include: livenessState
        readiness:
          include: readinessState,db

  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

  prometheus:
    metrics:
      export:
        enabled: true
